# Native acceptance — 2026-08-29

This run used the public `main` branch on Windows without Docker:

- Node.js 22.15.0 and Java 17;
- PostgreSQL 17.11 on `127.0.0.1:5432`;
- MinIO `RELEASE.2025-09-07T16-13-09Z` on `127.0.0.1:9000`;
- `pnpm control:native` for Spring Boot, Vite, and the Worker.

## Results

- Native prerequisite checks passed and the Web UI bound to `127.0.0.1:3000`.
- An interval schedule enqueued its immediate job, paused, resumed, and remained enabled after a full control-plane restart.
- A documented five-field Cron expression (`*/15 * * * *`, `Asia/Shanghai`) was accepted, calculated its next run, and enqueued an immediate manual run.
- A published workflow with two independent source nodes completed both source jobs and merged them into deterministic normalize, gate, and report nodes.
- The workflow finished as `completed`; all five durable checkpoints were `completed`, with queued, finalizing, and completed events recorded.
- `pnpm check` passed TypeScript tests, Java tests, typecheck, frontend lint, and the production web build.

The run intentionally did not claim external-model, browser-CDP, credentialed-provider, Docker, or cluster acceptance. Those remain unchecked in [`ROADMAP.md`](../../ROADMAP.md).
